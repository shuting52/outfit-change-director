---
name: outfit-change-director
description: >-
  一键换装卡点导演：基于参考图片生成超写实网红名媛舞蹈卡点换装视频。用户上传参考图并选择换装方式和舞种风格，自动生成角色一致的参考图、完整视频脚本和视频。支持12种换装过渡方式（M1–M12）和14种舞种卡点风格。触发词：换装视频、卡点换装、舞蹈换装、一键换装、outfit change video、dance outfit swap。
metadata:
  execution_mode: sandbox
---

# 一键换装卡点导演

基于用户上传的参考图片，一句话生成超写实网红名媛舞蹈卡点换装视频。

## User-Facing Output

保持回复聚焦于用户任务和可见结果。不暴露内部命名空间、来源标签、路由元数据或执行模式值。发送回复前移除内部运行时细节，用用户可见的操作、结果和决策来描述。

## 工作流程

### Step 1：接收用户输入

用户需提供：
1. **参考图片**（必需）— 包含人物形象的照片
2. **简短描述**（必需）— 一句话说明想要的效果
3. **换装方式**（可选，默认 M1）— 从 M1–M12 中选择
4. **舞种风格**（可选，默认"万能"）— 从14种舞种中选择

若用户未指定换装方式或舞种，主动展示选项菜单：

**换装过渡方式：**

| 编号 | 名称 | 适用场景 |
|------|------|----------|
| M1 | 点击＋完整人物飞入 | 重合拼贴、悬浮贴图 |
| M2 | 长袖／袖摆遮镜 | 汉服、宽袖服装 |
| M3 | 旋身接力 | 礼服、优雅转身 |
| M4 | 披帛／丝带缠绕 | 神女、仙侠 |
| M5 | 团扇／折扇遮挡 | 古风贵女 |
| M6 | 花瓣／云纹／水墨扩散 | 国风幻想 |
| M7 | 镜面／倒影同步 | 高级时尚、概念片 |
| M8 | 贴纸翻页／人物剪影翻转 | Lookbook、拼贴 |
| M9 | 分身归位融合 | 多人格衣橱、角色短片 |
| M10 | 步伐／跺脚／手势卡点 | 舞蹈、现代快节奏换装 |
| M11 | 发簪／耳饰／配饰触发 | 配饰识别度高的造型 |
| M12 | 衣纹生长／刺绣蔓延 | 汉服、刺绣服装 |

**舞种卡点风格：**
俏皮 · 迷人 · 柔美 · 万能 · 性感 · 性感2 · 甜酷 · 欧美 · 拉丁 · 韩系 · 串烧 · 巴西 · Ama · 俏皮2

若用户只需要纯舞蹈视频（不换装），跳过换装方式选择，仅选舞种风格。

### Step 2：分析参考图片

调用 `image_understand` 分析参考图片，提取：
- 人物五官、发型、肤色、体型等身份特征
- 当前服装细节
- 场景/背景信息
- 侧边贴纸位置和颜色（如有）

将提取的特征写入人物一致性描述，替换核心约束模板中的默认描述。

### Step 3：组装视频生成提示词

1. 读取 `references/core-constraints.md` 获取人物一致性、镜头场景、服装表现、背景音乐的基础约束模板
2. 根据用户选择的换装方式，读取 `references/transition-methods.md` 获取对应的换装过渡脚本段落
3. 如果用户选择了舞种风格（非换装模式），读取 `references/dance-styles.md` 获取对应的舞蹈编排脚本
4. 将 Step 2 提取的人物特征替换模板中的 `{人物描述}` 占位符
5. 组装完整提示词：核心约束 + 换装/舞蹈脚本 + 音乐指令

**组装规则：**
- 换装模式（M1–M12）：生成 8 秒 9:16 换装视频，包含 4 次换装卡点
- 纯舞蹈模式：生成 15 秒 9:16 舞蹈视频，不换装
- 混合模式：用户可同时选择换装方式 + 舞种风格，用舞种身体动作替换默认编排，保留换装过渡机制

### Step 4：生成参考图

使用 `imagen_generate` 基于用户参考图生成符合视频脚本的起始帧参考图：
- 9:16 竖版构图
- 人物居中全身
- 穿第一套服装
- 包含四个侧边贴纸（换装模式下）
- 场景与参考图一致

将生成的参考图展示给用户确认。**必须等用户确认后才进入下一步。**

### Step 5：生成视频

用户确认参考图后，调用 `video_generate` 生成视频：
- 将完整提示词作为 prompt
- 参考图作为 image input
- 时长：换装模式 8 秒 / 舞蹈模式 15 秒
- 比例：9:16

### Step 6：生成背景音乐（可选）

若用户未提供音乐，调用 `music_generate` 生成配套卡点音乐：
- BPM 与视频节拍匹配
- 在换装卡点时间设置强重拍
- 风格匹配选择的舞种

### Step 7：交付

将视频上传交付给用户，附带：
- 使用的换装方式和舞种风格
- 视频时间线卡点说明
- 建议的优化方向

## 关键规则

1. **人物一致性最高优先** — 全程同一人物，不换脸、不变形
2. **卡点精准对齐** — 换装动作必须与音乐重拍完全同步
3. **参考图忠实度** — 严格继承参考图中的人物特征和场景
4. **用户确认门控** — 参考图生成后必须等用户确认才进入视频生成
5. **一次只处理一个视频** — 不批量生成

## 引用文件

| 文件 | 何时读取 | 内容 |
|------|----------|------|
| `references/core-constraints.md` | Step 3 组装提示词时 | 人物一致性、镜头场景、服装、音乐的完整约束模板 |
| `references/transition-methods.md` | 用户选择换装方式时 | M1–M12 换装过渡方式的详细脚本 |
| `references/dance-styles.md` | 用户选择舞种风格时 | 14种舞种卡点风格的完整编排脚本 |
